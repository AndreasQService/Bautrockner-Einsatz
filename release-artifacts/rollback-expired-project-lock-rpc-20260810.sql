alter function public.acquire_project_lock(text, text, text, text, text, text)
  security invoker;

alter function public.acquire_project_lock(text, text, text, text, text, text)
  set search_path = public;
